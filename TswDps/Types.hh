#pragma once

enum class SubType {
    None,
    Burst,
    Strike,
    Focus,
    Frenzy
};

enum class DmgType {
    Melee,
    Magic,
    Ranged
};

enum class SkillType {
    None,
    Builder,
    Consumer
};

enum class PassiveType {
    None,
};
