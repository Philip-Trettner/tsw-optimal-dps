#pragma once

enum class SubType {
    None,
    Burst,
    Strike,
    Focus,
    Frenzy
};

enum class DmgType {
    None,
    Melee,
    Magic,
    Ranged
};

enum class SkillType {
    None,
    Builder,
    Consumer
};

enum class PassiveType {
    None,
};
