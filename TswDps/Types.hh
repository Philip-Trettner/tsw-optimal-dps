#pragma once

enum class SubType {
    None,
    Burst,
    Strike,
    Focus,
    Frenzy
};

enum class DmgType {
    None,
    Melee,
    Magic,
    Ranged
};

enum class SkillType {
    None,
    Builder,
    Consumer,
    PassiveFullHit,
    Proc
};

enum class PassiveType {
    None,
};
